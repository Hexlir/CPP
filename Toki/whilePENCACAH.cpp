#include "bits/stdc++.h"

using namespace std;

int main(int argc, char const *argv[])
{
    short A;
    int sum = 0;

    while(scanf("%hd", &A) != EOF){
        sum += A;
    }

    cout << sum << endl;
    return 0;
}
