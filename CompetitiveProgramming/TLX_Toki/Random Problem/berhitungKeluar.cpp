#include "iostream"

#define endl '\n'
#define fastIO \
    cin.tie(NULL); \
    cout.tie(NULL); \
    std::ios::sync_with_stdio(false);

using std::cin;
using std::cout;

int main(){
    int n,m,x;
    cin >> n >> m >> x;
}