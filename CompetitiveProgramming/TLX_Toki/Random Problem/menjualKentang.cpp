#include "iostream"

#define endl '\n'

using std::cin;
using std::cout;

int main(){
    int n,m;
    cin >> n >> m;

    int a[n + 3], b[m + 3];
    for (int i = 0; i < n; i++){
        cin >> a[i];
    }
    for (int i = 0; i < m; i++){
        cin >> b[i];
    }

    int ans = 0;

    
}