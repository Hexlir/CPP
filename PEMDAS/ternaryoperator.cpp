#include <bits/stdc++.h>

using namespace std;

int main(){
    int a, b;
    cout << "Masukkan nilai a & b : ";
    cin >> a >> b;

    cout << "Nilai terkecil adalah : " << (a < b ? a : b) << endl;
    cout << "Nilai terbesar adalah : " << (a > b ? a : b) << endl;
}