#include "bits/stdc++.h"

using namespace std;

int main(){
    int row;
    cin >> row;

    while (true){
        do {
            cout << "Halo" << endl;
            row--;
        } while (row > 0);
        return 0;
    }
}